// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.0'
**BUILD_SCRIPT_DEPS**}
}

allprojects {
    repositories {
        google()
        jcenter()
        // SDK远程仓库配置
        maven {
            url "http://nexus.rastarops.com:8999/repository/RastarGameSdkOversea/"
        }
        flatDir {
            dirs 'libs'
        }
    }
}

apply plugin: 'com.android.application'
**APPLY_PLUGINS**

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    // SDK核心功能模块，必接
    implementation 'com.rastargame.sdk.oversea.na:rastar-sdk-core-hk:3.0.8'
    // SDK AppsFlyer数据统计模块，选接
    implementation 'com.rastargame.sdk.oversea.na:rastar-sdk-appsflyer:3.0.7'
//    // SDK Google模块，选接
    implementation 'com.rastargame.sdk.oversea.na:rastar-sdk-google:3.0.0'
    // SDK Facebook模块，选接
     implementation 'com.rastargame.sdk.oversea.na:rastar-sdk-facebook:3.0.0'
//    // SDK LiveOps推送模块，选接
//    implementation "com.rastargame.sdk.oversea.na:rastar-sdk-liveops:2.1.5.3"
//    // SDK Naver模块，选接
//    implementation "com.rastargame.sdk.oversea.na:rastar-sdk-naver:2.1.5.3"
//    // SDK Line模块，选接
//    implementation "com.rastargame.sdk.oversea.na:rastar-sdk-line:2.1.5.3"

//    // SDK 三星渠道模块，选接（接入这个渠道只能使用三星支付，不能和其他渠道模块共存）
//    implementation "com.rastargame.sdk.oversea.na:rastar-sdk-samsung:2.1.5.3"
//    // SDK 小米渠道模块，选接（接入这个渠道只能使用小米登录和小米支付，不能和其他渠道模块共存）
//    implementation "com.rastargame.sdk.oversea.na:rastar-sdk-xiaomi:2.1.5.3"
//    // SDK ONE Store渠道模块，选接（接入这个渠道只能使用ONE Store支付，不能和其他渠道模块共存）
//    implementation "com.rastargame.sdk.oversea.na:rastar-sdk-onestore:2.1.5.3"
**DEPS**}

android {
    compileSdkVersion **APIVERSION**
    buildToolsVersion '**BUILDTOOLS**'

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    defaultConfig {
        minSdkVersion **MINSDKVERSION**
        targetSdkVersion **TARGETSDKVERSION**
        applicationId '**APPLICATIONID**'
        ndk {
            abiFilters **ABIFILTERS**
        }
        versionCode **VERSIONCODE**
        versionName '**VERSIONNAME**'

        multiDexEnabled true
         manifestPlaceholders.putAll([
                 FACEBOOK_APP_ID: '1496435593826813', // 接入FACEBOOK时填写FACEBOOK APP ID
        ])
    }

    lintOptions {
        abortOnError false
    }

    aaptOptions {
        noCompress = ['.unity3d', '.ress', '.resource', '.obb'**STREAMING_ASSETS**]
    }**SIGN**

    buildTypes {
        debug {
            minifyEnabled **MINIFY_DEBUG**
            useProguard **PROGUARD_DEBUG**
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'**USER_PROGUARD**
            jniDebuggable true
        }
        release {
            minifyEnabled **MINIFY_RELEASE**
            useProguard **PROGUARD_RELEASE**
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'**USER_PROGUARD****SIGNCONFIG**
        }
    }**PACKAGING_OPTIONS****SPLITS**
**BUILT_APK_LOCATION**
    bundle {
        language {
            enableSplit = false
        }
        density {
            enableSplit = false
        }
        abi {
            enableSplit = true
        }
    }
}**SPLITS_VERSION_CODE****REPOSITORIES****SOURCE_BUILD_SETUP**
